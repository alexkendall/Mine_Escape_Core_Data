//
//  timer.swift
//  Mine_Escape
//
//  Created by Alex Harrison on 5/24/15.
//  Copyright (c) 2015 Alex Harrison. All rights reserved.
//

// custom class that adds pause and resume functionality to nstimer 
// allows us to pause and resume game will retaining timer information

import Foundation

class Timer:NSTimer
{
    /*
    var toFire_interval = NSTimeInterval();
    
    func pause()
    {
        println("pausing");
        self.fireDate = NSDate(timeIntervalSinceReferenceDate: Double.infinity);
        var paused_date = NSDate();
        toFire_interval = fireDate.timeIntervalSinceDate(paused_date);
        assert(toFire_interval >= 0.0, "ToFire is negative when initialized!");
    }
    func resume()
    {
        println("resuming");
        self.fireDate = NSDate(timeInterval: toFire_interval, sinceDate: NSDate());
    }
    */
}